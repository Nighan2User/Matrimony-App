// lib/core/app_constants.dart

import 'package:flutter/material.dart';

// For consistent spacing and padding
class AppSizes {
  static const double small = 8.0;
  static const double medium = 16.0;
  static const double large = 24.0;
}

// For consistent colors - Modern matrimony palette
class AppColors {
  static const Color primaryColor = Color(0xFFD4436A); // Elegant rose
  static const Color accentColor = Color(0xFFB91C5A);
  static const Color textColor = Color(0xFF1E293B);
}